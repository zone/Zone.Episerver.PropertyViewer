@model Zone.Episerver.PropertyViewer.Models.PropertyListModel

@Html.LabelFor(m => m.PropertyName)
@Html.DropDownListFor(m => m.PropertyName, new SelectList(Model.PageProperties), "Select property\u2026")
