@using System.Web.Mvc.Html
@inherits System.Web.Mvc.WebViewPage<EPiServer.Core.PropertyContentReference>

@if (Model != null && Model.ContentLink != null)
{
    @Html.Action("RenderContentReference", "PropertyViewer", new { contentReference = Model.ContentLink })
}